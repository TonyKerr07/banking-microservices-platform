Contexto resumido (use este contexto para tomar decisões)
- Usuário: Antonio (não usa GitHub há algum tempo pq estava trabalhando no Banco BV e quer criar um projeto do zero para enriquecer seu GitHub a fim de que recrutadores tenham uma boa impressão de seus códigos e o posicionem como Desenvolvedor Senior).
- Objetivo: gerar do zero um projeto **Java 21 + Spring Boot 3.2.5** multi-módulo (se vc achar que multi-módulo é mais interessante para se posicionar como senior, senão pode alterar) Maven chamado **banking-microservices-platform**, pronto para colar no IntelliJ e subir ao GitHub.
- O usuário já criou a estrutura de módulos e renomeou pacotes base para `br.com.antonio.banking.<module>`. já existem os modulos: common, accounts-service, transfers-service, boletos-service, pix-service, api-gateway. Mesmo assim se achar que deve ser um unico modulo pode alterar, só leve em consideração que o usuário quer mostrar conhecimento em microsserviços.
- O usuário quer usar contratos OpenAPI (YAML) como fonte da verdade para gerar código (codegen) durante o build; **não** commitar artefatos gerados em `target/` pois ele usava assim no banco bv, caso necessário podemos desconsiderar os contratos YAML
- Ambiente alvo: IntelliJ (JDK 21 já instalado), Docker, Postman (para testes manuais). Forneça instruções para cada ferramenta.

Instruções principais para você (Claude)
1. **Projete a arquitetura** de forma sênior e pragmática. Você pode propor ajustes.
2. **Gere todos os arquivos** do projeto: `pom.xml` raiz, `pom.xml` de cada módulo (caso necessário), classes Java, resources, migrations Flyway, OpenAPI YAMLs (caso ache que deve ser por contratos mesmo), Dockerfiles, docker-compose, .gitignore, GitHub Actions workflow, README.md (raiz) e README por módulo se fizer sentido.
3. Para cada arquivo, **indique o caminho relativo** (ex. em caso de modulo: `accounts-service/src/main/java/br/com/antonio/banking/accounts/AccountsApplication.java`) e **o conteúdo completo** do arquivo.
4. Configure **OpenAPI codegen** (openapi-generator-maven-plugin) para gerar fontes em `target/generated-sources/openapi` durante a fase `generate-sources` se for decidido que é melhor por contrato. Explique claramente como gerar localmente e no CI.
5. Configure o **POM pai** para centralizar versões (dependencyManagement), Java 21 e pluginManagement (spring-boot-maven-plugin). Filhos devem declarar apenas dependências que usam se usar multi modulos.
6. Use **Flyway** para migrações; H2 para dev, PostgreSQL para produção (docker-compose deve incluir Postgres).
7. Inclua **testes básicos**: `@SpringBootTest` context load e alguns testes unitários simples (JUnit 5).
8. Inclua **Dockerfile** por serviço e `docker-compose.yml` na raiz para dev (Postgres + accounts-service como exemplo).
9. Inclua **Git workflow** sugerido (branches, mensagens de commit atômicas) e comandos exatos para gerar, buildar, rodar e subir ao GitHub.
10. Explique claramente a política de **contratos**: onde ficam os YAMLs, como versionar, quando (e por que) não commitar generated code, e como integrar codegen no CI se for usar contratos.
11. Forneça **mensagens de commit sugeridas** e um exemplo de PR description para a primeira entrega.
12. Se algum arquivo for muito grande, divida a resposta em partes e indique “CONTINUAR” no final de cada parte; continue até entregar tudo.

Requisitos técnicos e convenções (obrigatórios)
- Java 21
- Spring Boot 3.2.5
- Maven (decida caso for usar multi modulo ou não)
- GroupId: `br.com.antonio.banking`
- Versão do projeto: `1.0.0-SNAPSHOT`
- Pacote base se for ter módulo: `br.com.antonio.banking.<module>`
- Testes: JUnit 5 + Spring Boot Test, caso necessário futuramente podemos fazer com Karate também.
- Migrations: Flyway (migrations em `src/main/resources/db/migration`)
- Codegen: OpenAPI generator (contratos em `src/main/resources/openapi/*.yaml`) se decidido usar contratos.
- Docker: Dockerfile por serviço; docker-compose para dev
- CI: GitHub Actions com build, codegen e testes
- .gitignore: incluir `.idea/`, `target/`, `*.iml`, `*.log`, `*.DS_Store`
- Não commitar: `target/`, `target/generated-sources/*`, artefatos gerados

Entregáveis mínimos (o que eu quero que você gere agora)
- `pom.xml` (raiz) com módulos listados caso necessário e dependencyManagement
- decisão do projeto - como será realizado e todas as features que achar importante ter para o banco atual e mostrar conhecimento - levando em consideração que já existem esses módulos (common, accounts-service, transfers-service, boletos-service, pix-service, api-gateway) vc pode ter uma ideia do que o usuário está esperando.
- `docker-compose.yml` (raiz) com Postgres e accounts-service
- `.github/workflows/ci.yml` com build + codegen + tests
- `README.md` (raiz) profissional, com badges, instruções de execução, roadmap e como contribuir
- `.gitignore` (raiz)
- todo o esqueleto de pastas para o usuário criar no intelliJ
- criar todas as classes e indicar o caminho que elas devem ser adicionadas.

Detalhes sobre o uso do OpenAPI contract case necessário (explique para o desenvolvedor)
- Onde colocar os YAMLs (por módulo em `src/main/resources/openapi/`)
- Como configurar o plugin para gerar fontes em `target/generated-sources/openapi`
- Comandos para gerar localmente: `mvn -pl accounts-service -am clean generate-sources`
- Como integrar no CI: executar `mvn clean install` na pipeline para garantir geração reproduzível
- Política: **contrato versionado** no repo; **artefatos gerados não versionados**; PRs para mudanças de contrato; se necessário, publicar um pacote de contratos separado (opcional)

Comandos exatos que o usuário deve executar (incluir exemplos)
- Clonar, criar branch, gerar, buildar, rodar:
  - `git clone <repo-url>` - o usuário ja tem o repositório (https://github.com/TonyKerr07/banking-microservices-platform)
  - `git checkout -b feature/accounts-service` exemplo para branch de criação de conta
  - `mvn -pl accounts-service -am clean generate-sources`
  - `mvn -B clean install`
  - `mvn -pl accounts-service -am spring-boot:run`
  - `docker-compose up --build` (quando usar Docker)
- Comandos para CI local e debug (ex.: `mvn -DskipTests=false test`)

Política de commits e PR (exemplos) se achar que isso é nivel senior
- Mensagens atômicas e com escopo: `feat(accounts): add Account entity and repository`
- Exemplo de PR description para a primeira entrega (objetivo, o que foi feito, como testar, próximos passos)

Observações finais (o que o usuário espera)
- O usuário quer que você **decida a arquitetura** sênior do zero (camadas, patterns, DTOs, exceptions, uso de UUID vs Long, transações)
- O usuário usará Claude para gerar o código; ele quer **arquivos prontos** para colar no IntelliJ. Se algo precisar de ajuste manual, explique claramente o porquê e como ajustar.
- Seja pragmático: entregue skeletons bem estruturados, defina se serão utilizados módulos e gere os códigos de cada classe.
- Ao final, liste os próximos 8 tarefas/commits recomendados para evoluir o projeto (curto prazo).

Resposta esperada do Claude
- gerar e explicar o esqueleto de pastas para o desenvolvedor gerar.
- Gerar todos os arquivos com caminhos e conteúdo exato.
- Incluir instruções de execução, geração de código, Docker e CI.
- Incluir mensagens de commit e PR example.
- Se não couber em uma resposta, dividir e continuar até completar.